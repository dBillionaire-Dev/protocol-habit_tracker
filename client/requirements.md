## Packages
framer-motion | Smooth layout transitions and micro-interactions
date-fns | Robust date formatting and manipulation
lucide-react | Iconography
clsx | Conditional class names
tailwind-merge | Merging tailwind classes safely

## Notes
- App uses Replit Auth for authentication.
- "Avoidance" habits track debt (bad).
- "Build" habits track streaks/penalties (good).
- Dates should be formatted as YYYY-MM-DD for API requests.
- Dark mode is the default and preferred aesthetic.
